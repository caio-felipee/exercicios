#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int n, t;
    cin >> n;
    
    ll ans = 0, even = 0, odd = 0;
    for(int i = 0; i < n; i++)
    {
        cin >> t;

    }

    cout << ans << "\n";
}
